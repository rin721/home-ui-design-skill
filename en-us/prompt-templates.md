# Prompt Templates

## Homepage

```text
Use home-ui-design-skill to create a homepage for [audience]. Include a compact top navigation, calm hero, modular content cards, optional sidebar or secondary widgets, clear CTA, responsive behavior, and accessibility notes. Use neutral placeholder copy where content is missing.
```

## Landing Page

```text
Use home-ui-design-skill to create a landing page for [offer]. Include hero, feature cards, proof section, product or service details, CTA panel, footer, design tokens, interaction states, and mobile-first layout rules.
```

## Product Introduction Section

```text
Use home-ui-design-skill to generate a product introduction section for [product category]. Include section heading, short benefit copy, feature list, media block, CTA, token usage, responsive behavior, and accessibility requirements.
```

## Mobile Page

```text
Use home-ui-design-skill to design a mobile page for [task]. Prioritize compact top navigation, icon controls, full-width cards, readable headings, touch targets, floating panels, reduced motion, and neutral placeholder content.
```

## Component Specification

```text
Use home-ui-design-skill to create component specs for [components]. For each component include usage, visual structure, token dependencies, states, responsive behavior, accessibility, implementation hints, forbidden patterns, and a neutral example.
```

## Design Tokens

```text
Use home-ui-design-skill to generate semantic design tokens for [interface type]. Include color, typography, spacing, radius, shadow, border, motion, breakpoint, z-index, opacity, blur, container, and grid tokens. Include role, usage, confidence, modifiable, and fallback for each token.
```

## React Implementation

```text
Use home-ui-design-skill to create a React implementation for [page or component]. Define token CSS variables, semantic components, props for states, responsive behavior, keyboard support, and reduced-motion handling.
```

## Vue Implementation

```text
Use home-ui-design-skill to create a Vue implementation for [page or component]. Define token CSS variables, component props, class bindings for states, responsive behavior, keyboard support, and reduced-motion handling.
```

## HTML CSS Implementation

```text
Use home-ui-design-skill to create an HTML/CSS implementation for [page or component]. Use semantic landmarks, CSS variables, rounded card surfaces, compact controls, responsive media queries, visible focus, and reduced-motion rules.
```

## Tailwind Implementation

```text
Use home-ui-design-skill to create a Tailwind implementation plan for [page or component]. Map design tokens to Tailwind theme values, use semantic component classes where useful, include responsive variants, states, and accessibility requirements.
```

## Color Style Replacement

```text
Use home-ui-design-skill to replace the primary hue with [hue or color direction]. Keep card geometry, spacing, hierarchy, and interaction behavior stable. Recheck contrast, focus visibility, dark mode, and selected states.
```

## Component Style Replacement

```text
Use home-ui-design-skill to adjust component style toward [technical, premium, minimal, corporate, or product-led]. Keep tokens semantic, preserve accessibility, and list which tokens changed.
```

## Quality Check

```text
Use home-ui-design-skill to validate this UI output. Check token usage, layout grammar, component recipes, states, responsive behavior, accessibility, neutral content, private asset avoidance, and release hygiene.
```
